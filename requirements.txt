# OpenShift & SNO Support Copilot — Python Dependencies
# Pinned to match Developer A's requirements.txt exactly for shared container build.

fastapi==0.139.0
uvicorn[standard]==0.49.0
pydantic==2.13.4
pydantic-settings==2.14.2
python-dotenv==1.2.2

# OpenSearch
opensearch-py==3.2.0

# IBM watsonx.ai
ibm-watsonx-ai==1.5.14

# LangGraph / LangChain
langgraph==1.2.7
langchain-core==1.4.8

# PDF parsing (Developer B — ingestion pipeline)
pdfminer.six==20231228

# Config / serialization
pyyaml==6.0.3
httpx==0.28.1

# Observability
structlog==26.1.0

# IBM COS (ingestion pipeline — optional, falls back to local if not set)
ibm-cos-sdk>=2.13.0

# Testing
pytest==9.1.1
pytest-asyncio==1.4.0
